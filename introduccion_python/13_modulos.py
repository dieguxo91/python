# Modulos

from module import said_Hello # por si solo queremos importar una función en concreto

import module # no acepta numeros en el nombre del modulo
import math # podriamos importar librerias existentes como en otros lenguajes

module.sum(3,2)

module.said_Hello()

print(math.floor(4.2))